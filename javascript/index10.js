// var car;
//objecs to store multiple variables and name values

// creating object and storing property value and property name
// printing in console.log

const car = {
  // car = object name
  name: "fiat", // name = property name, fiat = property value
  model: 2007, // model = property name, 2007 = property value
  color: "black", // color = property name, black = property value
};
console.log(car.name);
console.log(car);
console.log(car.model);
const person = {
  //object name
  personName: "smith", //property name = propety value
  personAge: 58, //property name = propety value
  personColor: "fair", //property name = propety value
  personWeight: "63", //property name = propety value
};
console.log(person.personName);
console.log(person.personAge);
console.log(person.personColor);
console.log(person.personWeight);
console.log(person);

// create object
// property name, property value assign
// property name = function (stored as a value)
// property name = john smith = function() = firstname + lastname and show me as full name = output = value
// concat first john , last smith, john+smith , john smith, concatination, concat

const patient = {
    patientFname: "john",
    patientLname: "Doe", // "string 1" + "    " +  "string 2"
    patientAge: 58,
  }; 
 